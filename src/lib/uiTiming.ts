/** 팝업(디데이/책 추가/시간 등) 트랜지션 길이 — styles.css `:root` 의 `--dur-modal` 과 동일하게 유지 */
export const MODAL_TRANSITION_MS = 360;

/** 하단 네비게이션 기반 메인 페이지 전환의 체감 완료 시점 */
export const PAGE_TRANSITION_SETTLE_MS = 220;

/** 동일 셸 내부 탭 전환의 체감 완료 시점 */
export const TAB_TRANSITION_SETTLE_MS = 180;
